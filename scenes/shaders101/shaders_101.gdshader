shader_type canvas_item;


void vertext() {
	int x = 10;
	x = int(2.2);
	float y = 2.5;
	bool z = true;
	
	vec4 my_vect = vec4(1.0, 2.0, 3.0, 4.0);
	//my_vect.x, y, z, w
	//my_vect.r, g, b, a
	//my_vect.s, t, p, q
	
	vec3 my_vec3 = vec3(1.0);
	
	// ivec2 - i for int
	// bvec2 - b for bool
}

void fragment() {
	//COLOR.rb = vec2(0.0);
}